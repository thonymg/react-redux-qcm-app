/* eslint-disable */
import React from 'react';
import { shallow } from 'enzyme';

import Question from './';

describe('<Question />', () => {
  it('renders', () => {
    shallow(<Question />);
  });
});
