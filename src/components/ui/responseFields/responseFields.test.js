/* eslint-disable */
import React from 'react';
import { shallow } from 'enzyme';

import ResponseFields from './';

describe('<ResponseFields />', () => {
  it('renders', () => {
    shallow(<ResponseFields />);
  });
});
